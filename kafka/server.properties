# Adjust the session timeout to allow more time for consumers to send heartbeats
session.timeout.ms=30000

# Adjust the heartbeat interval to match the session timeout
heartbeat.interval.ms=10000

# Other relevant properties
leader.heartbeat.interval.ms=1000
request.timeout.ms=40000